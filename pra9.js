console.log("kunal")
let a=10;
let a=100;

// we got error in this program because in js we can initialization let more than one time if want to initizlize again than use only variable name example a=10